<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item>
        <shape android:shape="rectangle">
            <corners
                android:bottomRightRadius="@dimen/cornerlessround"
                android:topRightRadius="@dimen/cornerlessround"
                android:bottomLeftRadius="@dimen/cornerlessround"
                android:topLeftRadius="@dimen/cornerlessround"
                />
        </shape>
    </item>

    <item android:top="0dp" android:bottom="0dp" android:left="0dp">
        <shape
            android:shape="rectangle">

            <solid android:color="@color/loginbutton" />
            <corners
                android:bottomRightRadius="@dimen/cornerlessround"
                android:topRightRadius="@dimen/cornerlessround"
                android:bottomLeftRadius="@dimen/cornerlessround"
                android:topLeftRadius="@dimen/cornerlessround"
                />
        </shape>
    </item>

</layer-list>